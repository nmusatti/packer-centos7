{
  "description": "Build CentOS 7 x86_64 workstation",
  "variables": {
      "proxy": "{{ env `http_proxy` }}"
  }, 
  "builders": [
    {
      "communicator": "ssh",
      "source_path": "centos7-vbga",
      "output_dir": "./centos7-gui",
      "box_name": "centos7-gui",
      "provider": "virtualbox",
      "skip_add": true,
      "type": "vagrant"
    } 
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "scripts/gui.sh"
      ],
      "environment_vars": [
        "http_proxy={{ user `proxy` }}"
      ],
      "override": {
        "vagrant": {
          "execute_command": "echo 'vagrant'|sudo -S {{ .Vars }} bash '{{.Path}}'"
        }
      }
    }
  ]
}
